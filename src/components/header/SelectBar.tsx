import React from "react"
import { Button } from "@material-ui/core"

function SelectBar() {
    return (
        <div>
            <Button variant="contained" color="secondary">New page</Button>
            <Button variant="contained" color="secondary">New group</Button>
        </div>
    )
}

export default SelectBar
